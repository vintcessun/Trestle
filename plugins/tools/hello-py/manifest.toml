name = "hello-py"
version = "0.1.0"
kind = "tool"
description = "验证 Python 也能写 Trestle 插件"

[capabilities]
# 刻意**不**开 stateless。这个插件本身确实无状态，但 componentize-py 产出的
# 组件是 18 MB，池化就是 ×N 内存。它是演示用的，不值得。
# stateless = true

